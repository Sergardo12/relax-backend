services:
  - type: web
    name: relax-backend
    env: docker
    plan: free
    dockerContext: .
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: relax-db

databases:
  - name: relax-db
    plan: free
